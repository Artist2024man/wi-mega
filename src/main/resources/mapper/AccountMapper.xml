<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wuin.wi_mega.repository.mapper.AppAccountMapper">

    <select id="sumByUserId" resultType="java.math.BigDecimal">
        SELECT sum(cur_equity) FROM app_account WHERE user_id = #{userId}
        <if test="exchange != null and exchange != ''">
            AND exchange = #{exchange}
        </if>
        AND deleted = 0;
    </select>

    <select id="listUserIds" resultType="java.lang.Long">
        SELECT distinct user_id FROM app_account WHERE deleted = 0;
    </select>
</mapper>
